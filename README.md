# PointerAI - Landing Page

Современная лендинг-страница, созданная с помощью Next.js, React и Tailwind CSS.

## 🚀 Деплой на GitHub Pages

Этот проект настроен для автоматического деплоя на GitHub Pages через GitHub Actions.

### Шаги для деплоя:

#### 1. Создайте репозиторий на GitHub

1. Перейдите на [GitHub](https://github.com) и войдите в свой аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу и выберите **"New repository"**
3. Заполните данные:
   - **Repository name**: `pointer-ai` (или любое другое имя)
   - **Description**: Описание проекта (опционально)
   - Выберите **Public** или **Private**
   - **НЕ** добавляйте README, .gitignore или license (они уже есть в проекте)
4. Нажмите **"Create repository"**

#### 2. Настройте GitHub Pages

1. Перейдите в репозиторий на GitHub
2. Откройте **Settings** (в верхней панели репозитория)
3. В левом меню найдите раздел **Pages**
4. В разделе **Source** выберите:
   - **Deploy from a branch**: выберите `gh-pages` или **GitHub Actions** (рекомендуется)
5. Сохраните изменения

#### 3. Инициализируйте Git и отправьте код

Выполните следующие команды в терминале:

```bash
# Инициализируйте git репозиторий (если еще не сделали)
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: Setup for GitHub Pages deployment"

# Добавьте remote репозиторий (замените YOUR_USERNAME и REPO_NAME на ваши данные)
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Переименуйте ветку в main (если у вас master)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

#### 4. Автоматический деплой

После отправки кода на GitHub:

1. GitHub Actions автоматически запустит workflow при каждом push в ветку `main`
2. Вы можете увидеть процесс деплоя в разделе **Actions** вашего репозитория
3. После успешного деплоя ваш сайт будет доступен по адресу:
   - `https://YOUR_USERNAME.github.io/REPO_NAME/`

Если вы хотите использовать корневой домен (без `/REPO_NAME`), вам нужно:
- Создать репозиторий с именем `YOUR_USERNAME.github.io`
- Обновить `next.config.mjs` и убрать `basePath` (если он был добавлен)

### 📝 Важные замечания

1. **Ветка по умолчанию**: Убедитесь, что ваша ветка по умолчанию называется `main`. Если у вас `master`, обновите `.github/workflows/deploy.yml` и измените `main` на `master`.

2. **Vercel Analytics**: В проекте используется `@vercel/analytics`, который может не работать на GitHub Pages. Если нужна аналитика, рассмотрите альтернативные решения.

3. **Первая деплой**: Первый деплой может занять несколько минут. Проверьте статус в разделе **Actions**.

## 🛠️ Разработка

### Установка зависимостей

```bash
npm install
# или
pnpm install
```

### Запуск в режиме разработки

```bash
npm run dev
# или
pnpm dev
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

### Сборка для продакшена

```bash
npm run build
# или
pnpm build
```

Статические файлы будут созданы в папке `out/`.

## 📦 Технологии

- **Next.js 15** - React framework
- **React 19** - UI библиотека
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **Framer Motion** - Анимации
- **Radix UI** - UI компоненты

## 📄 Лицензия

Этот проект является приватным.

