# Инструкция по созданию репозитория и деплою на GitHub Pages

## Шаг 1: Создание репозитория на GitHub

### Вариант A: Через веб-интерфейс GitHub

1. Откройте [GitHub.com](https://github.com) и войдите в аккаунт
2. Нажмите кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните:
   - **Repository name**: `pointer-ai` (или ваше имя)
   - **Description**: (опционально)
   - Выберите **Public** или **Private**
   - **ВАЖНО**: НЕ добавляйте README, .gitignore или license (они уже есть!)
4. Нажмите **"Create repository"**
5. Скопируйте URL репозитория (будет показан на следующей странице)

### Вариант B: Через GitHub CLI (если установлен)

```bash
gh repo create pointer-ai --public --source=. --remote=origin --push
```

## Шаг 2: Инициализация Git и отправка кода

Выполните команды в терминале (замените URL на ваш):

```bash
# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Создайте первый коммит
git commit -m "Initial commit: Setup for GitHub Pages"

# Добавьте remote репозиторий
# ЗАМЕНИТЕ YOUR_USERNAME и REPO_NAME на ваши данные!
git remote add origin https://github.com/YOUR_USERNAME/REPO_NAME.git

# Убедитесь, что вы используете ветку main
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

## Шаг 3: Настройка GitHub Pages

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** → **Pages** (в левом меню)
3. В разделе **Source**:
   - Выберите **"Deploy from a branch"** → выберите **"GitHub Actions"**
   - ИЛИ выберите ветку `gh-pages` (если она появится после первого деплоя)
4. Сохраните изменения

## Шаг 4: Проверка деплоя

1. Перейдите в раздел **Actions** вашего репозитория
2. Вы увидите процесс деплоя (может занять 2-5 минут)
3. После успешного деплоя сайт будет доступен по адресу:
   - `https://YOUR_USERNAME.github.io/REPO_NAME/`

## ⚠️ Важные замечания

- Если ваша ветка по умолчанию называется `master` (а не `main`), обновите `.github/workflows/deploy.yml`:
  - Замените `main` на `master` в строке 6

- Если вы хотите использовать корневой домен (без `/REPO_NAME`):
  - Создайте репозиторий с именем `YOUR_USERNAME.github.io`
  - Оставьте `basePath` закомментированным в `next.config.mjs`

- Для последующих изменений:
  ```bash
  git add .
  git commit -m "Your commit message"
  git push
  ```
  Деплой произойдет автоматически через GitHub Actions!

## 🆘 Проблемы?

- **Ошибка деплоя**: Проверьте раздел **Actions** → выберите failed workflow → посмотрите логи
- **404 на сайте**: Убедитесь, что `basePath` настроен правильно в `next.config.mjs`
- **Сайт не обновляется**: Подождите 1-2 минуты, затем обновите страницу с Ctrl+F5

